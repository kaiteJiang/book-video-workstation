# Contributing

感谢你愿意改进 BV Workstation。这个项目优先接受范围小、能复现、能验证的改动。

## 开始之前

1. 先开 Issue 说明问题、期望行为和复现方法。
2. 不要提交电子书正文、私人笔记、语音样本、成片或供应商原始响应。
3. 不要把 API Key、Token、Cookie、签名 URL 或本机配置写进代码、测试和文档。
4. 依赖升级、工作流门禁改变和外部服务接入，请单独提交。

## 本地验证

```powershell
uv sync --group dev
uv run pytest -q
```

提交前再运行 `git diff --check`，并确认新增文件没有敏感信息。测试外部服务时，请使用本地环境变量和自己的合法账号，PR 中只保留脱敏结果。

## Pull Request

请说明改了什么、为什么改、如何验证、还有哪些边界没有覆盖。不要顺手重构无关代码，也不要把格式化全仓库和功能修改放在同一个 PR。
