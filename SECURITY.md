# Security Policy

## 报告安全问题

请通过 GitHub 的私密漏洞报告功能联系维护者。不要在公开 Issue、Discussion、PR、截图或日志里粘贴密钥、Cookie、私有媒体、签名 URL 和供应商完整响应。

如果你怀疑凭据已经进入 Git 历史，请先吊销并轮换凭据，再私下提供受影响的文件路径和提交范围。不要把真实凭据复述到报告中。

## 项目边界

外部 TTS、ASR、图像生成和视频生成都应由当前请求显式授权。仓库默认忽略 `.env`、`config.yaml` 和 `workspace/` 下的书籍、语音、日志、缓存与临时文件，但使用者仍需在提交前自行检查暂存区。
