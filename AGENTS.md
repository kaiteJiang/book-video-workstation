# BV Workstation 项目规则

## 当前创作方式

- 文稿以事件、冲突、应对、结果推进；第一视角不等于内心感慨。删除重复感受、无依据的心理戏和强加升华，让观众从剧情因果获得理解。配音统一 `speed=1.12`，以实际新音频重做时间轴。

- 新的图书内容采用长篇故事路线。根据故事价值、因果、转折和完整性决定长度，常用预算 6–8 分钟，优先控制在 10 分钟内；不凑时长，超时先审查支线与节奏。
- 以书中人物第一视角为主。明确其在各时点知道什么；不适合时采用稳定第三视角。改写独白与推演不得伪称原著引语或史实。
- 从全书选择最有价值的核心故事，必要支线服务同一个人物变化。不要求把全书人物和观点塞进同一篇。
- 不使用去 AI 味 Skill，不调用 human-writing 或其检查器。新故事走 `bv.content.story` 的直接写作、证据、复查和批准路径；旧 value 模式只用于历史兼容。

## 生产真源与门禁

- 先读 `docs/runbooks/longform-story-production.md`。长篇 AB 的故事语义必须使用 `skills/bv-story-unit-ab/SKILL.md`；画幅、标题、字幕和转场遵循 `skills/bv-book-video-style-contract/SKILL.md` 及其视觉合同。故事小节计划校验通过后才能规划媒体，缺失时不得退回旧自然段/微动作分镜。
- 每篇 Episode 的 `production_profile.json`、`episode.json`、`script/story_candidate.json` 和实际文件哈希是状态依据。旧短稿和已有媒体不覆盖。
- 用户批准升级流程不等于批准新写出的文稿。先完整稿件待审，再声音试听，再代表 AB 图，再补齐/渲染/终审。不在文稿待审阶段生成 TTS、ASR、图片、分镜媒体或成片。
- `bv import-story` 导入正文和复查元数据，只登记候选；`bv approve <book_id> <episode_id> script` 才批准当前候选。不得伪造已通过的检查或批准记录。
- 音色从 `src/bv/voice/data/doubao_voice_catalog.json` 筛选；名称/ID 来源核验、实际账户可用性、试听通过和热度是不同事实。候选目录不代表实际试听通过。
- AB 组数按全文语义选择，最终配音及 ASR 是时间轴。每对先 A 后 B，B 引用当前 A 的路径与 SHA-256，跨组维持同一人物和场景身份。
- 长篇 AB 表达一个叙事小节的起点与高潮/结果，不是同一镜头内几秒钟的手势变化。A 到 B 之间必须有正文支持的事件发展、处境变化与因果；允许有依据地跨时间、地点、年龄、服饰与机位，锁定的是人物身份和故事连续性。旧视觉 Skill 中“同地点同机位、只推进一个动作”的限制不再适用于长篇。不能仅延长静帧停留来冒充剧情跨度。
- 不自动上传。外部调用按当前用户授权与所在门禁执行；只读状态查询优先，不把 `bv next` 当成无副作用查询。

## 验证与交付

- 功能变更运行对应单元/集成测试；长片测试使用本地 fixtures，不借测试消耗外部额度。
- 区分候选文稿、候选音色、候选视觉与实际合格媒体。交付列出准确路径、当前停点与未验证事项。
- 未经用户请求不更新全局记忆、不提交或推送项目、不更改其他作品的生产状态。


## 本地作品状态

恢复已有作品时先读 `workspace/production-state.md`（若存在），再核对当前制作指针与真实清单。该文件不随公开仓库分发；其中记载不代替文件哈希或用户验收。
